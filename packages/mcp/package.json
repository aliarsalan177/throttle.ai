{
  "name": "@tre/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the TRE context store + compress tools (phase 2 / M5).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@tre/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
