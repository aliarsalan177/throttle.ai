{
  "name": "@tre/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the TRE context store + compress tools.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "bin": {
    "tre-mcp": "./dist/index.js"
  },
  "dependencies": {
    "@tre/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
